# TODO: Google Calendar Alarm App

Цей файл — повна дорожня карта розробки програми, яка читає події з Google Calendar і програє звук у потрібний момент.

## 0) Поточний стан (вже зроблено)

- [x] Підключено `googleapis`, `dotenv`, `play-sound`
- [x] Є `requiredEnv` перевірка в `calendar.js`
- [x] Налаштовано OAuth2 через refresh token (`createAuthClient`)
- [x] Є polling кожні 60 секунд (`setInterval`)
- [x] Є нормалізація старту подій (`normalizeEventsStart`)
- [x] Є дедуплікація через `Set` (`worked`)
- [x] Є перевірка існування mp3 перед відтворенням

## 1) Базова стабільність MVP (P0)

### 1.1. Надійний пошук подій
- [ ] Переглянути `maxResults` (зараз 5) і встановити безпечніше значення (наприклад 20-50)
- [ ] Переконатися, що `singleEvents: true` і `orderBy: "startTime"` залишаються увімкненими
- [ ] Вирішити, чи потрібен `timeMax` (наприклад `now + 24h`) для обмеження запиту

**DoD:**
- [ ] Подія, створена на найближчі хвилини, стабільно потрапляє в `events`

**Перевірка:**
- [ ] Запустити застосунок і створити тестову подію на +2 хв

### 1.2. Вікно спрацювання
- [ ] Підтвердити правило: alarm спрацьовує, якщо подія в межах `now..now+60s`
- [ ] Зафіксувати це правило коментарем у коді

**DoD:**
- [ ] Подія на +30с спрацьовує; подія на +2хв не спрацьовує зараз

### 1.3. Поведінка при кількох подіях в один тік
- [ ] Залишити поточну бізнес-логіку: у тіку грає тільки перша релевантна подія
- [ ] Явно описати це в коді/коментарі

**DoD:**
- [ ] При 2+ подіях на той самий тік звучить лише 1 alarm

## 2) Дедуплікація та пам'ять (P0)

### 2.1. Безпечна дедуплікація
- [ ] Зберігати в `worked` не тільки `event.id`, а ключ виду `id + startTimestamp`
- [ ] Це потрібно, щоб повторні події одного `id` у різний час не блокувалися

**DoD:**
- [ ] Повторювані події (recurring) спрацьовують у різні дні

### 2.2. Очищення `worked`
- [ ] Додати очищення старих ключів (наприклад раз на добу)
- [ ] Або обмежити розмір структури (простий retention)

**DoD:**
- [ ] Після тривалої роботи пам'ять не росте безконтрольно

## 3) All-day події (P1)

### 3.1. Політика для `isAllDay`
- [ ] Визначити бізнес-правило: 
  - або ігнорувати all-day
  - або спрацьовувати в заданий локальний час (наприклад 09:00)
- [ ] Додати окрему функцію обчислення trigger-time для all-day

**DoD:**
- [ ] All-day події поводяться передбачувано за обраним правилом

## 4) Обробка помилок і логування (P0)

### 4.1. Єдині повідомлення логів
- [ ] Привести логи до одного стилю: `[INFO]`, `[WARN]`, `[ERROR]`
- [ ] Логувати `event.id`, `summary`, `start`

### 4.2. Поведінка при збої API
- [ ] Якщо Google API тимчасово недоступний, застосунок не падає
- [ ] Додати обмеження шуму логів (не дублювати однакові помилки щосекунди)

**DoD:**
- [ ] Короткочасний збій мережі не ламає постійний polling

## 5) Тести (P1)

### 5.1. Unit-тести на чисті функції
- [ ] `normalizeEventsStart` (dateTime/date/invalid)
- [ ] Логіка визначення "подія в межах вікна"
- [ ] Логіка побудови dedup-key

### 5.2. Мінімальний інтеграційний smoke-test
- [ ] Мок `calendar.events.list`
- [ ] Перевірка: при релевантній події викликається `alarm`

**DoD:**
- [ ] `npm test` проходить локально

## 6) Конфігурація і безпека секретів (P0)

### 6.1. Секрети
- [ ] Тримати секрети тільки в `.env`
- [ ] Додати/перевірити `.gitignore` для `.env`, `node_modules`, локальних логів
- [ ] Створити `.env.example` без реальних значень

### 6.2. Ротація ключів
- [ ] Після навчальних тестів перевипустити `client secret` і `refresh token`
- [ ] Оновити `.env` новими значеннями

**DoD:**
- [ ] У репозиторії немає реальних токенів/секретів

## 7) DX та запуск (P1)

### 7.1. Скрипти в `package.json`
- [ ] `start`: запуск `calendar.js`
- [ ] `dev`: запуск з детальнішими логами (за потреби)
- [ ] `test`: запуск тестів

### 7.2. Документація
- [ ] Додати `README.md` з розділами:
  - встановлення
  - налаштування `.env`
  - запуск
  - типові помилки

**DoD:**
- [ ] Нова людина може запустити проєкт за 10-15 хвилин

## 8) Git-процес і реліз (P1)

### 8.1. Робочий процес
- [ ] Працювати у коротких гілках `feature/...`
- [ ] Коміти робити атомарними (1 логічна зміна = 1 коміт)

### 8.2. Перед push
- [ ] Перевірити, що `.env` не відстежується git
- [ ] Запустити локальні тести/смоук
- [ ] Описати зміну зрозумілим commit message

### 8.3. Якщо `git push` відхилено
- [ ] `git pull --rebase origin main`
- [ ] вирішити конфлікти
- [ ] `git rebase --continue`
- [ ] `git push`

## 9) Definition of Done (стабільна версія)

Вважаємо програму стабільною, коли:
- [ ] Вона працює без падінь під час тривалого запуску (наприклад 24 години)
- [ ] Alarm спрацьовує передбачувано для тестових подій
- [ ] Немає повторного alarm для тієї самої події/тіка
- [ ] Секрети не потрапляють у git
- [ ] Є базові тести і `README`

---

## Найближчі 3 кроки (що робити прямо зараз)

1. [ ] Закрити P0 у блоці 1: `maxResults`, правило вікна, поведінка "перша подія за тік".
2. [ ] Закрити P0 у блоці 2: dedup-key (`id + start`) і очищення `worked`.
3. [ ] Закрити P0 у блоці 6: `.env.example` + перевірка, що секрети не в git.

